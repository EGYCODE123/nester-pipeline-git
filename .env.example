# Server Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_LOG_LEVEL=info

# CORS Configuration
# Use * to allow all origins (for public API access)
# Or specify domains: https://app1.com,https://app2.com
API_ALLOWED_ORIGINS=*

# Security - REQUIRED: Generate a strong random key
# Generate with: openssl rand -hex 32 (Linux/Mac)
# Or PowerShell: -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | ForEach-Object {[char]})
API_KEY=your-secure-api-key-here-change-this

# Rate Limiting
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_BURST=20

# Logging
LOG_DIR=logs

# Note: Railway automatically provides PORT environment variable
# The application will use PORT if available, otherwise falls back to API_PORT or 8000
